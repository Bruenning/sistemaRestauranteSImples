<?php

use Illuminate\Http\Request;
use Illuminate\Support\Facades\Route;
use App\Http\Controllers\Api\UsersController;

// Rota para fazer login
// Route::post('/login', [UsersController::class, 'login'])->name('login');

// Rota para fazer logout
// Route::post('/logout', [UsersController::class, 'logout'])->middleware('auth:api');

// Rota para registrar um novo usuário
// Route::post('/register', [UsersController::class, 'register']);

// Rota para recuperar a senha do usuário
// Route::post('/password/reset', [UsersController::class, 'resetPassword']);

// Rota para redefinir a senha do usuário
// Route::post('/password/change', [UsersController::class, 'changePassword'])->middleware('auth:api');
